package pl.kacpermajkowski.teamup.listeners;

import org.bukkit.event.EventHandler;
import org.bukkit.event.Listener;
import pl.kacpermajkowski.teamup.events.TeamCreateEvent;

public class TeamCreateListener implements Listener {
    @EventHandler
    public void onTeamCreation(TeamCreateEvent event) {
    }
}
